# Copy to .env (gitignored) and set a real value. Never commit .env.
# Falls back to 'admin' in docker-compose.yml if unset — fine for local dev,
# not for anything reachable beyond your own machine.
GRAFANA_ADMIN_PASSWORD=changeme
